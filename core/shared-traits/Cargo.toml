[package]
name = "shared-traits"
description = "Shared traits for the Logos Ecosystem"
version = "0.1.0"
edition = "2024"
license.workspace = true

[features]
# Exposes the substrate contract suite to store implementations.
test-support = []

[dependencies]
crypto = { workspace = true }
thiserror = "2"
